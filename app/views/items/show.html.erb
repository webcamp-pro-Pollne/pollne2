<div class="container">
  <div class="row">
    <div class="col">
      <%= attachment_image_tag @item, :image, :fill, 60, 60, fallback: "no_image.png" %>
    </div>
    <div>
      <h2><%= @item.name %></h2>
      <p><%= @item.introduction %></p>
      <p>¥<%= @item.tax_price %>(税込)</p>
    </div>
  </div>
<%= render "search", genres:@genres %>
</div>